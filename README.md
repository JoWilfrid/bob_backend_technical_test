# Drivy technical test

## Setup

* `gem install bundler --no-ri --no-rdoc` (if you're running [RVM](http://rvm.io/) or you havn't bundler installed for any reason)
* `bundle install`

## Run tests

`bundle exec rspec`

## Run script

`bundle exec ruby bin/run`
