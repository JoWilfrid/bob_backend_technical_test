#!/usr/bin/env ruby

APPLICATION_ROOT = File.expand_path(Dir.pwd, '..')
$LOAD_PATH.unshift File.expand_path('app', APPLICATION_ROOT)

require 'level'

level = Level.new

level.run

puts level.result.inspect
